/**
 * Mini-C 编译器 - 常量折叠优化测试
 * 
 * 测试场景：
 * - 编译期常量计算
 * - 复杂表达式优化
 */

void main() {
    // 应优化为常量 23
    int a = 3 + 4 * 5;
    output_int(a);  // 预期：23
    
    // 应优化为常量 14
    int b = (2 + 3) * 2 + 4;
    output_int(b);  // 预期：14
    
    // 应优化为常量 100
    int c = 10 * 10;
    output_int(c);  // 预期：100
}

